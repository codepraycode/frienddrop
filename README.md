# frienddrop
LAN-first P2P file sharing (AirDrop alternative) with secure identity, granular permissions, resumable transfers, and optional relay mode. Built as a monorepo with React + Express + TypeScript.
