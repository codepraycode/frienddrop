# The port the host-agent API will listen on
HOST_PORT=4242

# The path to the SQLite database file
# For local dev, this defaults to './data/frienddrop.db' relative to process.cwd()
HOST_DB_PATH=./data/frienddrop.db

# Logging level
HOST_LOG_LEVEL=info
