# The port the host-agent API will listen on
HOST_PORT=4242

# The directory where the SQLite database will be stored
# For local dev, this defaults to './data/frienddrop.db' in the package folder if not set
HOST_DB_PATH=./data/frienddrop.db

# Logging level
HOST_LOG_LEVEL=info
